<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        Da bordo a bordo (D46): il contenuto passa sotto le barre di sistema, che sono
        trasparenti, e ogni schermata sposta dentro solo ciò che deve restare leggibile.
    -->
    <style name="Theme.InkNote" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:colorBackground">@color/desk</item>
        <item name="android:windowBackground">@color/desk</item>
        <item name="android:colorAccent">@color/accent</item>
        <item name="android:colorControlActivated">@color/accent</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

    <!--
        Il tema della cattura. windowAnimationStyle nullo non è estetica: ogni
        animazione di apertura è tempo che l'utente aspetta prima di poter scrivere
        (D19, D32). Il foglio è carta, chiaro anche di notte: le barre restano scure su
        chiaro.
    -->
    <style name="Theme.InkNote.Capture" parent="Theme.InkNote">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowContentTransitions">false</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

</resources>
